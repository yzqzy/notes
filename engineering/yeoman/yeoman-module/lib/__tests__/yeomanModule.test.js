const assert = require('assert');
const yeomanModule = require('../index.js');

describe('yeomanModule', () => {
  it('has a test', () => {
    assert(false, 'yeomanModule should have a test');
  });
});
